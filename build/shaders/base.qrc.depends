#version 330

layout(location = 0) in vec4 vPos;

// transformations
uniform mat4 MVproj; //model-view-projection mx

out vec4 theColor;

void main()
{
    gl_Position = vPos;//MVproj * vPos;
    theColor = vec4(1,0,0,1);
}
